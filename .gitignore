# Build output
build/
captures/
.externalNativeBuild
.cxx
*.apk
*.aab
*.ap_
*.dex

# Gradle / Kotlin caches
.gradle/
.kotlin/
local.properties
.gradletasknamecache

# Signing material — never commit these.
# A release keystore or its passwords in git history cannot be un-published;
# rotating a signing key means users can no longer update the app.
*.jks
*.keystore
keystore.properties
signing.properties
key.properties

# IDE
.idea/
*.iml
*.ipr
*.iws
.vscode/

# OS
.DS_Store
Thumbs.db

# Profiling / crash output
*.hprof

# Skiff Code web bundle: node_modules, and the build output that lands in assets
code/web/node_modules/
code/src/main/assets/web/
